# utilizando biblioteca 'math' para calcular a raiz quadrada de um número

import math

raiz = float(input('Digite um número: '))
print('Essa é a raiz quadrada do número {:.2f}: {:.2f}'.format(raiz, math.sqrt(raiz)))
